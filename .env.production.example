# FRbox Production Environment Variables
# Copy this file to .env and configure with your values

# ===== Image Processing =====
# Maximum image size in bytes (default: 2097152 = 2MB)
MAX_IMAGE_SIZE=2097152

# Maximum image width for resizing (default: 640)
MAX_IMAGE_WIDTH=640

# Maximum number of faces allowed per image (default: 1)
MAX_FACES=1

# ===== Embedding Settings =====
# Expected embedding dimension (default: 128, dlib standard)
EMBEDDING_DIM=128

# Default similarity threshold for face verification (default: 0.85)
SIMILARITY_THRESHOLD=0.85

# ===== Security Settings (IMPORTANT FOR PRODUCTION!) =====

# Generate secure API keys:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
# Multiple keys: separate with commas
# Empty = DISABLED (development mode only!)
API_KEYS=

# Comma-separated list of allowed CORS origins
# Empty = allow all origins (development mode only!)
# Example: https://example.com,https://app.example.com
ALLOWED_ORIGINS=

# Rate limit: requests per minute per API key or IP address
RATE_LIMIT_PER_MINUTE=60

# ===== Usage =====
# 1. Copy this file: cp .env.production.example .env
# 2. Edit with your production values
# 3. Run: docker compose -f docker-compose.production.yml --env-file .env up -d
